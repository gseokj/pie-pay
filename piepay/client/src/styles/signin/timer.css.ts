import { style } from '@vanilla-extract/css';

export const timerContainer = style({
  display: 'flex',
  width: '35%',
  justifyContent: 'space-between',
});

export const ment = style({});

export const remainTime = style({
  fontWeight: 'bold',
});
