export interface Store {
    address: string;
    phone: string;
    storeName: string;
}

export interface Menu{
    menuName: string;
    menuPrice: number;
    quantity: number;
    total: number;
}